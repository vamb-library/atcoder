#include <bits/stdc++.h>
using namespace std;

int main(void){
    int n, a,b; cin >> n >> a >> b;
    int p,q,r,s; cin >> p >> q >> r >> s;

    vector<vector<char>> box(n,vector<char>(n,'.'));
    
}